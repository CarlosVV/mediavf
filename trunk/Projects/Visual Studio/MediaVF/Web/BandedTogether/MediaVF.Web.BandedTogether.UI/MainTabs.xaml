<UserControl x:Class="MediaVF.Web.BandedTogether.UI.MainTabs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctl="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
    xmlns:region="clr-namespace:Microsoft.Practices.Prism.Regions;assembly=Microsoft.Practices.Prism"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot" Background="White">
        <ctl:TabControl Name="MainTabsControl" region:RegionManager.RegionName="MainRegion">
            <region:TabControlRegionAdapter.ItemContainerStyle>
                <Style TargetType="ctl:TabItem">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock Text="{Binding Header}" />
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </region:TabControlRegionAdapter.ItemContainerStyle>
        </ctl:TabControl>
    </Grid>
</UserControl>
